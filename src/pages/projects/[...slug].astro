---
import WorkDetailPage from '../../components/pages/WorkDetailPage.astro';
import { getProjectPaths, type ProjectEntry } from '../../lib/content';

export async function getStaticPaths() {
  return getProjectPaths('ko');
}

const { project } = Astro.props as { project: ProjectEntry };
---
<WorkDetailPage project={project} lang="ko" />
